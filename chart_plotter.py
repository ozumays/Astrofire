# chart_plotter.py
# ==============================================================================
# ⚠️ BU MODÜL ARTIK PASİFTİR.
# Çizim işlemleri (SVG) tamamen Frontend (layout.html / JavaScript) tarafına taşınmıştır.
# Matplotlib bağımlılığı kaldırılmıştır.
# Bu fonksiyonlar sadece eski kodların hata vermemesi için "boş" (dummy) olarak tutulmaktadır.
# ==============================================================================

def create_chart_image(chart_data, output_path):
    """
    ARTIK KULLANILMIYOR.
    Çizim işlemi tarayıcıda (JavaScript) yapılıyor.
    """
    print("DEBUG: Backend çizimi (create_chart_image) devre dışı. Frontend kullanılıyor.")
    return False

def create_synastry_chart(chart1_data, chart2_data, output_path):
    """
    ARTIK KULLANILMIYOR.
    Sinastri çizimi tarayıcıda (JavaScript) yapılıyor.
    """
    print("DEBUG: Backend sinastri çizimi (create_synastry_chart) devre dışı. Frontend kullanılıyor.")
    return False